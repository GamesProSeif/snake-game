{
  "name": "snake",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "build:dev": "tsc -w",
    "deploy": "push-dir --dir=public --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "p5": "^1.3.1"
  },
  "devDependencies": {
    "@types/p5": "^0.9.1",
    "push-dir": "^0.4.1",
    "typescript": "^4.3.2"
  }
}
